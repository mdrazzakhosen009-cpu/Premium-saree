# SAREE Premium
Express + Turso + Groq. Upload the files to the GitHub root. Render build: `npm install`; start: `npm start`.
Required Render env: TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, ADMIN_PASSWORD, GROQ_API_KEY. Images are stored as data URLs in Turso so Render redeploys do not remove them.
